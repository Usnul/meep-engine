export class MarkerNodeTransformer {
    /**
     *
     * @param {MarkerNode} node
     * @param {GridData} grid
     */
    transform(node, grid) {
        throw new Error(`Not implemented`);
    }
}

export class MarkerNodeTransformer {
    /**
     *
     * @param {number} seed
     */
    initialize(seed) {

    }

    /**
     *
     * @param {MarkerNode} node
     * @param {GridData} grid
     */
    transform(node, grid) {
        throw new Error(`Not implemented`);
    }
}

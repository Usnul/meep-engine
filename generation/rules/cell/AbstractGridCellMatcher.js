export class AbstractGridCellMatcher {
    /**
     *
     * @param {GridData} grid
     * @param {number} x
     * @param {number} y
     * @param {number} rotation
     * @returns {boolean}
     */
    match(grid, x, y, rotation) {

        return true;
    }
}
